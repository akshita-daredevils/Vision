PYTHON_VERSION=3.10.12

# Comma-separated CORS allowlist (no spaces)
FRONTEND_ORIGINS=https://432f323ere.pages.dev,https://vision-8.onrender.com

# Optional: model location override (defaults to backend/flood_classifier.h5)
# MODEL_PATH=/opt/render/project/src/backend/flood_classifier.h5

# Backend port (Render provides $PORT automatically; use locally if needed)
PORT=4000

# Performance guardrails (tune to keep analysis under ~30s)
FRAME_STRIDE=2
MAX_FRAMES=360
MAX_PROCESS_SECONDS=25

# Firebase service account (if used server-side)
FIREBASE_PROJECT_ID=water-velocity-project
FIREBASE_CLIENT_EMAIL=
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"
FIREBASE_STORAGE_BUCKET=water-velocity-project.firebasestorage.app
FIREBASE_API_KEY=AIzaSyDskYJol705ucTtdbE3zZIBpgJRVsqt_aY

# Alert threshold used by backend when raising alerts
VELOCITY_ALERT_THRESHOLD=2.5
